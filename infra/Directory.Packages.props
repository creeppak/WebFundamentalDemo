<Project>
  <PropertyGroup>
    <!-- Infra manages its own package versions independently of the main solution. -->
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
  </PropertyGroup>
</Project>